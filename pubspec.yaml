name: dcc_app
description: A minimal Flutter app scaffold for DCC App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ^1.2.2
  flutter_dotenv: ^5.1.0
  provider: ^6.1.2
  shared_preferences: ^2.3.2
  uuid: ^4.5.0



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
